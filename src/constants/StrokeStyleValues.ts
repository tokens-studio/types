export enum StrokeStyleValues {
  SOLID = 'solid',
  DASHED = 'dashed',
  DOTTED = 'dotted',
  DOUBLE = 'double',
  GROOVE = 'groove',
  RIDGE = 'ridge',
  OUTSET = 'outset',
  INSET = 'inset',
}

export type StrokeStyleValuesUnion = `${StrokeStyleValues}`;
